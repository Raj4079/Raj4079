service product{
    function printHelloWorld(input: String) returns String;

    function addItmes(num1: Integer,num2:Integer) returns Integer;

    @open
    type object {};
    function myFunction(element:Integer) returns object;
}